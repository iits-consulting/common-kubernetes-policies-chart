# This test causes the helm test-suite to fail. We can't test for the expected failure of something in helm.
#
# apiVersion: v1
# kind: Pod
# metadata:
#   name: "unsigned-image-denied"
#   annotations:
#     "helm.sh/hook": test
# spec:
#   containers:
#     - name: signed-image
#       image: "iits/git-jq:1.0.0"
#       command: ['echo']
#       args: [ "hello" ]
#   restartPolicy: Never